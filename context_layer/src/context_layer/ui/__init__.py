"""Real-time dashboard. Pure Python via NiceGUI — no HTML or JSX.

Renders each pipeline stage as it completes so the neurosymbolic override is
visible live. See app.py for the security note: no authentication, localhost only.
"""
