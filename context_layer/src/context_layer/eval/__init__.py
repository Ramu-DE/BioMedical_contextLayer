"""Evaluation: proving the context layer beats naive RAG. Phase 7, Requirement 11.

    baseline.py    vector-only control arm, no governance
    questions.py   question set with declared expected behaviour
    harness.py     runs both arms, scores identical metrics
    adversarial.py conflicting info, pattern pollution, signal drowning
"""
